matplotlib==3.8.4
numpy==1.26.4
Pillow==10.4.0
typing-extensions==4.12.2
fastapi==0.115.0
uvicorn[standard]==0.30.6
pyyaml==6.0.2

# 云存储: 多实例部署时把产物外置到 CloudBase 云存储(底层 COS)。
# 未配置 COS_* 环境变量时不会导入, 自动回落到本地盘单实例模式。
cos-python-sdk-v5==1.9.30
