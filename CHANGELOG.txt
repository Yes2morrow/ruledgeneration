0.2.2 - 2026-09-22

- Fix concave containment, consistent quarter-turn transforms, per-module artwork
  and bed coordinates; keep target external group spacing at zero.
- Replace bed-only connectivity with explicit internal aisles, real openings and
  1.2 m public roads. Allow an explicitly reported 1.0 m fallback when it fits more
  beds. Reject openings against walls, site edges or insufficient public clearance.
- Treat G's middle barrier as impassable and its two aisles separately; restore
  E's missing upper aisle branch without changing module or bed dimensions.
- Try opposite-facing entrances and existing group decomposition when necessary.
  Recommendation trials now use the same road rules as final generation.
- Cache unchanged opening geometry and bed/aisle contacts; omit display-only
  unions during candidate checks. Final audit always runs.
- Validation: 35 Python regressions, 3 preview tests, 3 end-to-end cases, and
  64 mixed-site geometry/cache-consistency audits. 64- and 100-bed examples fit
  completely with 1.2 m public roads; the U-site fits 8 beds with a 1.0 m fallback.
- See docs/release-0.2.2.md for actual images, model limitations and current
  performance. Earlier bed-only timing and connectivity claims are superseded.
- No cloud deployment. Release version: 0.2.2.

0.2.1

Fixes
- Report placed beds, modules and cost from the actual layout; retain requested
  totals in selectionSummary and report unplaced beds/modules explicitly.
- Render one single-module texture for each placed module. Extract A's original
  single module from its atlas; do not stretch B's two-bed artwork over a group.
- Rotate textures clockwise to match placement geometry and honor module mirrors.
- Draw bed geometry when artwork is absent. Serialize Matplotlib rendering to
  prevent concurrent requests from sharing figure/style state.

Validation
- python 04_test/test_plan_consistency.py: 6 regression tests.
- python 04_test/test_cli_modes.py: 3 existing end-to-end cases.
- 100-bed selection (A26/B4/C2/D4): 20x30 m places 62 beds and reports 38 unplaced;
  30x45 m places all 100 beds in 36 modules. B4 renders all 8 beds.
- Generated images inspected locally. No cloud deployment performed.
